{
  "name": "@ruvel/mission-hub",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@ruvel/mission-core": "workspace:*",
    "@ruvel/spike-core": "workspace:*"
  },
  "scripts": {
    "build": "vite build --configLoader runner && cd ../.. && node scripts/build-phase1-worker.mjs mission",
    "dev": "vite --configLoader runner --port 4180 --strictPort",
    "typecheck": "tsc --noEmit"
  }
}
