{
  "name": "@ruvel/spike-partner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": { "@ruvel/spike-core": "workspace:*" },
  "scripts": {
    "build": "vite build --configLoader runner && node ../../scripts/write-sites-worker.mjs partner",
    "dev": "vite --configLoader runner --port 4174 --strictPort",
    "typecheck": "tsc --noEmit"
  }
}
